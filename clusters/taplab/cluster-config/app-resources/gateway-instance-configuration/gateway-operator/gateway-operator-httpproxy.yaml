apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: gateway-operator
  namespace: default
spec:
  routes:
  - conditions:
    - prefix: /
    services:
    - name: scg-openapi-service
      namespace: spring-cloud-gateway
      port: 80
  virtualhost:
    fqdn: scg.a5.fynesy.com
    tls:
      secretName: taptlscert
